import { PhraseDirective } from './phrase.directive';

describe('PhraseDirective', () => {
  it('should create an instance', () => {
    const directive = new PhraseDirective();
    expect(directive).toBeTruthy();
  });
});
